// Messages (vi/en) + formatters (currency, date, plural).
// Follows next-intl convention; shared between web and admin.
// Key style: dot.namespaced kebab (e.g. checkout.error.out-of-stock) — see CLAUDE.md §7.
export {};
