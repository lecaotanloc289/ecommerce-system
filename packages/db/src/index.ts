// Prisma schema (DB source of truth), client singleton, seed, migrations.
// PrismaClient is NEVER exposed outside this package — see CLAUDE.md §6 NEVER DO.
// Only PrismaService (apps/api) consumes it.
export {};
