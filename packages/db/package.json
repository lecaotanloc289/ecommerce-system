{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "echo 'TODO: prisma generate'",
    "migrate": "echo 'TODO: prisma migrate dev'",
    "migrate:deploy": "echo 'TODO: prisma migrate deploy'",
    "reset": "echo 'TODO: prisma migrate reset'",
    "seed": "echo 'TODO: tsx src/seed.ts'",
    "lint": "echo 'TODO: eslint'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'TODO: vitest'"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "typescript": "^5.7.0"
  }
}
